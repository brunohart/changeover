/**
 * `release_hold`, under the name the backlog gives it. Owner: CORE-003.
 *
 * The verb is implemented in `release.ts`, which is the path the build
 * contract's ownership table names. Both names resolve to one function: R2's
 * totality is worth nothing if two modules disagree about what it is total
 * over.
 */

export type { ReleaseOutcome } from "./release.ts";
export { releaseHold } from "./release.ts";
